# Build output / Godot cache — dirty build output is the main cause of remote pull failures
.godot/
bin/
obj/
*.user
*.tmp

# Runtime data (central server SQLite database)
*.db
*.db-shm
*.db-wal

# Local assistant state
.aider*
